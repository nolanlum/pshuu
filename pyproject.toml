[project]
name = "pshuu"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "flask",
    "flask-cors",
    "pillow",
    "peewee",
    "uwsgi",
]

[dependency-groups]
dev = [
    "pytest>=8",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
