[project]
name = "pshuu"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "flask",
    "flask-cors",
    "pillow",
    "peewee",
    "uwsgi",
]

[dependency-groups]
dev = [
    "pytest>=9.1.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
